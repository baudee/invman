class: Transfer

table: transfer

fields:
  investment: Investment?, relation(onDelete=Cascade, name=investment_transfers)
  quantity: double
  amount: double

indexes:
  investment_idx:
    fields: investmentId