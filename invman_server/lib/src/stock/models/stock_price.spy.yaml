class: StockPrice

table: stock_price

fields:
  value: double,
  timestamp: DateTime,
  stock: Stock?, relation(onDelete=Cascade, name=stock_stock_prices)

indexes:
  stock_price_idx:
    fields: stockId, timestamp