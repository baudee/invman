class: Stock

table: stock

fields:
  symbol: String,
  name: String,
  value: double, default=0.0
  currency: String,
  quoteType: String
  transfers: List<Transfer>?, relation(name=stock_transfers)

indexes:
  stock_symbol_idx:
    fields: symbol
    unique: true
