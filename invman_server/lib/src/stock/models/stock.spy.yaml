class: Stock

table: stock

fields:
  symbol: String,
  name: String,
  value: double, default=0.0
  currency: String,
  stockExchange: String,
  exchangeShortName: String

indexes:
  stock_symbol_idx:
    fields: symbol
    unique: true
