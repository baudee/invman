class: StockLike

table: stock_like

fields:
  user: module:serverpod_auth_core:AuthUser?, relation(onDelete=Cascade)
  stock: Stock?, relation(name=stock_likes, onDelete=Cascade)
  createdAt: DateTime, default=now

indexes:
  stock_like_user_stock_idx:
    fields: userId, stockId
    unique: true
