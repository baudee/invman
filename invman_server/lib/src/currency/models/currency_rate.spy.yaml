class: CurrencyRate

table: currency_rate

fields:
  dollarValue: double,
  timestamp: DateTime,
  currency: Currency?, relation(onDelete=Cascade, name=currency_currency_rates)

indexes:
  currency_rate_idx:
    fields: currencyId, timestamp