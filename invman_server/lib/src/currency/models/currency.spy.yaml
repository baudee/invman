class: Currency

table: currency

fields:
  code: String
  dollarValue: double, defaultModel=0.0,
  timestamp: DateTime, defaultModel=now
  updatedAt: DateTime, defaultModel=now

indexes:
  currency_code_idx:
    fields: code
    unique: true