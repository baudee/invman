class: Currency

table: currency

fields:
  code: String
  rates: List<CurrencyRate>?, relation(optional, name=currency_currency_rates)

indexes:
  currency_code_idx:
    fields: code
    unique: true